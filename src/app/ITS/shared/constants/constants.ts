export enum DialogCloseEventType {
    Submit = 'submit',
    Cancel = 'cancel',
    Confirm = 'confirm'
}

export enum InitPageEventType {
    LOGIN_DONE = 'LOGIN_DONE',
    UPDATE_PAGE = 'UPDATE_PAGE',
    LOGGED_OUT = 'LOGGED_OUT',
    MAIN_MENU = 'MAIN_MENU'
}